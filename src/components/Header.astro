---
// Shared site header (logo + navigation + top bar).
// Edit this ONE file to change the header on every page.
// Markup is the exported Zoho theme header; styling comes from /css/zsite-core.css.
import raw from '../generated/header.html?raw';
---
<Fragment set:html={raw} />
